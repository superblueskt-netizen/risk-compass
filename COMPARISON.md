# AI 비교 — Card 5

이번 실습(T05)에서 실제로 관측된 AI A(Claude)와 AI B(Gemini)의 작업 기록을 같은 항목으로 정리합니다.

## 비교표

| 항목 | AI A (Claude) | AI B (Gemini) |
|---|---|---|
| 맡은 범위 | PNG 다운로드 기능 전체 구현 + 실배포 후 발견된 도넛차트 버그 수정 + 새 기능(파일명 개선) 착수 | 파일명 초 단위 중복 방지 (인계 문서에 명시된 남은 일 1건) |
| 요청 횟수 | 약 4회 (기능 구현 1회 + 버그 수정 1회 + 새 기능 착수 1회 + 검증 다수) | 1회 (인계 문서 요약 확인 → "진행해" → 완료 코드 제출까지 한 번에) |
| 인계 문서만 보고 이해했는가 | 해당 없음 (원 저장소 설계자) | 예 — 목표/현재상태/남은 문제(검사 11~14)를 정확히 요약, 오해 없이 바로 착수 |
| 통과시킨 검사 수 | 10개 (원 기능 PNG 다운로드, 검사 1~10) | 4개 (검사 11~14, 전체 통과) |
| 발견한 버그 | 1건 (도넛차트가 `html2canvas` 캡처에서 누락되던 실제 버그, 실배포 환경에서 발견 후 수정) | 0건 (버그 수정이 아닌 신규 기능 추가라 해당 없음) |
| 지시 범위를 벗어났는가 | 예 — 계획과 달리 정상 케이스까지 전부 완료하게 됨 (실사용 테스트 중 버그가 나와서) | 아니오 — 인계 문서에 정의된 범위(파일명 중복 방지)만 정확히 수정, 다른 코드는 한 글자도 안 건드림 (diff로 확인) |
| 재작업 발생 여부 | 있음 (계획 변경으로 WORKLOG·HANDOFF 재작성) | 없음 (한 번에 정확한 코드 제출, 재수정 요청 없었음) |

> 요청 횟수·소요 시간은 세션 로그를 기반으로 한 근사치이며, 초 단위로 정밀 측정된 값은 아닙니다.

## 다음 과제에서 사용할 선택 기준

1. **범위가 넓거나, 실배포 환경에서 실제로 검증하며 버그를 찾아가야 하는 작업에는 AI A(대화를 계속 이어가는 방식)부터 쓰고**, 작업 범위가 명확한 한 덩어리로 좁혀지면 그때 AI B로 넘긴다.
2. **인계 문서(HANDOFF.md)에 "건드리면 안 되는 부분"을 구체적으로 적어두면, AI B도 범위를 벗어나지 않고 한 번에 정확히 끝낸다** — 이번 실습에서 Gemini는 diff 기준으로 인계 문서에 명시된 파일명 로직 6줄 외에는 전혀 건드리지 않았다. 다음에도 인계 문서의 "건드리면 안 되는 부분"은 최대한 구체적으로 쓴다.
3. **작업 범위가 애매하거나 "일단 해보면서 발견되는" 성격의 일이 되면 AI A를 유지하고, 재작업이 반복되면(같은 이유로 2회 이상 다시 요청하게 되면) 작업을 더 잘게 쪼개서 AI B에게 넘기는 방식으로 바꾼다.**
