# AI 비교 — Card 5

이번 실습(T05)에서 실제로 관측된 두 AI의 작업 기록을 같은 항목·같은 검사(TESTS.md, 14개)로 정리합니다.
아래 판정표는 이름을 가린 상태로 먼저 봅니다 — 실제로 어떤 서비스였는지는 표 아래 "이름 공개"
섹션에서만 밝힙니다.

## 판정표 (이름 가림)

| 항목 | Model 1 | Model 2 |
|---|---|---|
| 역할 | 기능 설계·구현 시작(AI A) | 인계 문서만 보고 이어받아 완료(AI B) |
| 시작 버전 → 종료 버전 (커밋) | `ce6012f` → `3540f1d` | `3540f1d` → `4c2da40` |
| index.html 변경량 | +108 / -20줄 | +7 / -3줄 |
| 실제 작업시간 (사후 산정, WORKLOG.md 참조) | 약 33분28초 (2세션) | 약 10분31초 (1세션) |
| 실제 요청/호출 수 | 4회 | 1회 |
| 공통 상한 | 시간 40분 · 요청 10회 (동일 적용) | 시간 40분 · 요청 10회 (동일 적용) |
| 검사 통과 수 (14개 중) | 12개 (검사 1–10, 11, 12) | 14개 전체 재검증 통과 (신규 기여는 검사 13, 14) |
| 오류 수 (검사 1–10 기준, ≥1 FAIL 난 실행 회차 수) | 1회 (최초 실사용 테스트에서 도넛차트 캡처 버그 발견) | 0회 (검사 1–10 코드 미변경, diff로 확인) |
| 인계 문서만 보고 범위를 정확히 지켰는가 | 해당 없음 (원 설계자) | 예 — 인계 문서 명시 범위(파일명 로직) 외 한 글자도 변경하지 않음 (diff로 확인) |
| 재작업 발생 여부 | 있음 (실사용 중 버그 발견 → 계획 변경) | 없음 (1회 제출로 종료) |

> 요청 횟수·시간은 세션 로그(커밋 타임스탬프)를 근거로 한 근사치이며, 초 단위로 정밀 계측된 값은 아닙니다.

## 도구 선택 기준 (한 문장)

**작업 범위가 넓거나 실배포 환경에서 검증하며 버그를 찾아야 하는 단계는 Model 1(대화를 이어가며 붙잡고 있는 방식)에게 맡기고, "건드리면 안 되는 부분"까지 구체적으로 적은 인계 문서로 범위가 한 덩어리로 좁혀진 다음에만 Model 2(새 세션·새 모델)에게 넘긴다.**

### 부연 설명
1. 범위가 넓거나, 실배포 환경에서 실제로 검증하며 버그를 찾아가야 하는 작업에는 Model 1부터 쓰고, 작업 범위가 명확한 한 덩어리로 좁혀지면 그때 Model 2로 넘긴다.
2. 인계 문서에 "건드리면 안 되는 부분"을 구체적으로 적어두면, Model 2도 범위를 벗어나지 않고 한 번에 정확히 끝낸다 — 이번 실습에서 Model 2는 diff 기준으로 인계 문서에 명시된 파일명 로직(+7/-3줄) 외에는 전혀 건드리지 않았다.
3. 작업 범위가 애매하거나 "일단 해보면서 발견되는" 성격의 일이면 Model 1을 유지하고, 같은 이유로 재작업이 2회 이상 반복되면 그때 작업을 더 잘게 쪼개 Model 2에게 넘기는 방식으로 바꾼다.

## 이름 공개 (판정에는 사용되지 않음, 참고용)
- Model 1 = **Claude (Sonnet 5)** — AI A, 원 저장소 설계자
- Model 2 = **Gemini** — AI B, 인계 문서·저장소만 보고 이어받음 (다른 서비스/모델 조건 충족)
