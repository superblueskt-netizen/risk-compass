# AI A 작업 기록 (Card 2)

| 항목 | 내용 |
|---|---|
| AI | Claude (Sonnet 5) |
| 시작 시각 | 2026-08-25 00:55 UTC |
| 사용 요청 수 | 4 / 10 |
| 상한 도달 여부 | 아니오 |

## 원래 계획과 실제 진행이 달라진 부분 (투명하게 기록)
원래 계획은 "정상 케이스(검사 1–4번)는 의도적으로 미완성 상태로 두고 중단 → AI B가 이어받아 완성"이었습니다.
하지만 실제로 배포 후 사용자가 직접 테스트하는 과정에서 **진짜 버그(도넛차트가 캡처 이미지에서 통째로 빠짐 —
`html2canvas`가 CSS `conic-gradient`를 지원하지 못하는 게 원인)** 가 발견되었고, 같은 세션에서 AI A가 이를
직접 수정했습니다. 그 결과 원래 검사 1–10번이 모두 통과된 상태가 되어, AI B에게 넘길 "남은 일"이 사라졌습니다.

이를 보완하기 위해 **새로운 작은 개선 기능 하나를 추가로 정의**하고, 이번에는 계획대로 일부만 완료한 뒤
의도적으로 중단했습니다. (아래 "새 개선 기능" 참조)

## 원래 기능: PNG 다운로드 — 검사 결과 (10개 전체 통과)

| # | 구분 | 결과 |
|---|---|---|
| 1 | 정상: 기본 배분 다운로드 | 통과 (실제 배포 환경에서 사용자 직접 확인) |
| 2 | 정상: 슬라이더 조정 후 다운로드 | 통과 |
| 3 | 정상: 금액 변경 후 다운로드 | 통과 |
| 4 | 정상: 프로필별 다운로드 | 통과 |
| 5 | 오류: 계산 전 다운로드 시도 | 통과 |
| 6 | 오류: 금액 0/빈 값 | 통과 |
| 7 | 오류: 다운로드 차단 환경 | 통과 |
| 8 | 회귀: 슬라이더 재분배 | 통과 |
| 9 | 회귀: 실시간 위험유형/수익률 | 통과 |
| 10 | 회귀: 설문 다시하기 | 통과 |

### 발견 및 수정한 버그
- **증상**: 실제 배포 환경에서 다운로드한 PNG를 열어보니 도넛차트 부분이 완전히 비어 있었음
- **원인**: 도넛차트를 CSS `conic-gradient`로 그렸는데, `html2canvas`(1.4.1)가 이 최신 CSS 문법을 캡처하지 못함
- **수정**: `<div>` + CSS 그라디언트를 `<canvas>` + `ctx.arc()` 직접 그리기로 교체. `html2canvas`는 `<canvas>` 요소의
  픽셀 데이터를 그대로 복사하기 때문에 훨씬 안정적임
- **검증**: 사용자가 실제 배포 환경(시크릿 창)에서 재다운로드하여 도넛차트가 정상 표시됨을 확인

## 새 개선 기능 (AI B에게 인계할 작업)
사용자가 결과 이미지를 다운로드하면, 파일명에 위험유형과 투자금액이 포함되어
(예: portfolio_result_위험중립형_1000만원_2026-08-25.png) 여러 번 다운로드해도 구분할 수 있다.

### AI A가 완료한 부분
- 파일명에 위험유형 이름(safeRiskName)과 금액(amountLabel, 만원 단위 축약) 추가
- 파일명에 못 쓰는 특수문자(\ / : * ? " < > |) 제거 처리
- 샌드박스에서 파일명 생성 로직 자체는 단위 테스트로 검증 완료 (예: 적극투자형_1000만원, 550만원 케이스 등)

### 새 검사 항목 (검사 11–14번)
| # | 구분 | 결과 |
|---|---|---|
| 11 | 정상: 파일명에 위험유형이 포함된다 | 통과 (단위 테스트) |
| 12 | 정상: 파일명에 금액이 올바른 단위(만원)로 포함된다 | 통과 (단위 테스트) |
| 13 | 오류: 같은 날 두 번 이상 다운로드하면 파일명이 겹쳐서 이전 파일을 덮어쓴다 | 통과 (AI B가 초 단위 타임스탬프 추가로 해결, 헤드리스로 재검증 완료) |
| 14 | 회귀: 기존 이미지 캡처(도넛차트 포함) 기능이 그대로 유지된다 | 통과 (도넛차트 픽셀 렌더링 확인, 슬라이더/위험도 계산 로직 무변경 diff로 확인) |

## 남은 일 (AI B용)
1. index.html의 다운로드 클릭 핸들러 안 "TODO(다음 작업자):" 주석 부분을 찾아서,
   파일명에 초 단위 타임스탬프를 추가해 중복을 방지할 것 (검사 13번)
2. 실제 배포 환경에서 다운로드 → 도넛차트가 여전히 정상 표시되는지 확인할 것 (검사 14번)
3. 검사 11–14번이 모두 통과하면 완료

## AI B (Gemini) 작업 결과 — 검증 완료
- diff로 확인한 결과 다운로드 파일명 로직(7줄 추가/3줄 삭제, `git diff 3540f1d 4c2da40 -- index.html` 기준) 외 다른 코드는 전혀 변경되지 않음
- 도넛차트, 위험도 계산(CORR/RISK_BANDS), 슬라이더 재분배 로직 모두 무변경 확인
- 검사 11–14번 전체 헤드리스 브라우저로 재검증 통과
- 최종 파일명 예시: portfolio_result_적극투자형_1000만원_2026-08-25_080358.png

## 사전 상한 재구성 (2026-09-01 재검증 단계에서 사후 산정 — 아래는 소급 추정치임을 명시)
최초 작업 시점에는 "AI당 요청 10회"라는 상한만 명시적으로 기록되어 있었고, 시간 상한은 별도로
기록하지 못했습니다. 재검증 단계에서 커밋 타임스탬프를 근거로 아래와 같이 재구성했습니다.

| 구분 | AI A (커밋 기준) | AI B (커밋 기준) |
|---|---|---|
| 작업 세션 1 | `ce6012f`(00:55:25 UTC) → `4266608`(01:00:44 UTC) = 5분19초 | — |
| 작업 세션 2 | `871d997`(07:25:38 UTC) → `3540f1d`(07:53:47 UTC) = 28분9초 | `3540f1d`(07:53:47 UTC) → `4c2da40`(08:04:18 UTC) = 10분31초 |
| 실제 활성 작업시간(합) | 약 33분28초 | 약 10분31초 |
| 실제 요청/호출 수 | 4 / 10 | 1 / 10 |

**공통 시간 상한(사후 산정): AI당 40분.** 두 AI 모두 위 실제 작업시간이 40분 이하이므로 상한 내에서
완료된 것으로 간주합니다. (주의: 이 40분 기준은 최초 작업 착수 전에 문서화되지 않았고, 이번 재검증
시점에 실제 소요 시간을 근거로 사후 추정한 값입니다 — 다음 실습부터는 카드1 단계에서 사전에
명시적으로 기록합니다.)

## 저장소 버전(커밋) 요약 — AI A/AI B 시작·종료 지점
| 구간 | 시작 커밋 | 종료 커밋 | index.html 변경량 |
|---|---|---|---|
| AI A (PNG 다운로드 + 버그 수정 + 파일명 개선 착수) | `ce6012f` | `3540f1d` | +108 / -20줄 |
| AI B (파일명 초단위 중복 방지) | `3540f1d` | `4c2da40` | +7 / -3줄 |

## 오류 수 집계 방식 (검사 1–10 기준, 하나 이상 FAIL이 나온 실행 회차 수)
- AI A: 총 2회 실행 — 1회차(최초 배포 직후 실사용 테스트)에서 도넛차트 캡처 버그로 검사 1–4류가 실패,
  2회차(버그 수정 후 재검증)에서 전체 통과. **오류 수 = 1**
- AI B: 검사 1–10 코드는 diff상 전혀 손대지 않았고 별도로 재실행하지도 않음(범위 밖). **오류 수 = 0**
