# AI A 작업 기록 (Card 2)

| 항목 | 내용 |
|---|---|
| AI | Claude (Sonnet 5) |
| 시작 시각 | 2026-08-25 00:55 UTC |
| 종료(중단) 시각 | 2026-08-25 01:05 UTC |
| 소요 시간 | 약 10분 |
| 사용 요청 수 | 2 / 10 |
| 상한 도달 여부 | 아니오 — 계획된 지점(정상 케이스 통과)에서 의도적으로 중단 |

## 진행한 작업
1. `git init` 후 시작 상태 커밋 (`ce6012f`)
2. `<head>`에 html2canvas(CDN) 라이브러리 추가
3. 결과 패널(`#resultSection`)에 "🖼️ 결과 이미지로 저장" 버튼 추가
4. 클릭 시 슬라이더·버튼 등 조작 UI를 숨기고(`capturing` 클래스) `html2canvas`로 캡처 → PNG 다운로드
5. 계산 전 상태 가드, 라이브러리 로딩 실패 가드, try/catch 예외 처리 추가

## 검사 결과 (10개 중 진행 상황)

| # | 구분 | 결과 |
|---|---|---|
| 1 | 정상: 기본 배분 다운로드 | ⚠️ **미검증** (아래 "제약사항" 참조) |
| 2 | 정상: 슬라이더 조정 후 다운로드 | ⚠️ **미검증** |
| 3 | 정상: 금액 변경 후 다운로드 | ⚠️ **미검증** |
| 4 | 정상: 프로필별 다운로드 | ⚠️ **미검증** |
| 5 | 오류: 계산 전 다운로드 시도 | ✅ 통과 (버튼 자체가 노출되지 않음) |
| 6 | 오류: 금액 0/빈 값 | ✅ 통과 (기존 계산 검증 로직이 그대로 방어) |
| 7 | 오류: 다운로드 차단 환경 | ✅ 통과 (라이브러리 로딩 실패 시나리오로 대체 검증 — 크래시 없이 안내 문구 표시 확인) |
| 8 | 회귀: 슬라이더 재분배 | ✅ 통과 |
| 9 | 회귀: 실시간 위험유형/수익률 | ✅ 통과 |
| 10 | 회귀: 설문 다시하기 | ✅ 통과 |

## 제약사항 (다음 작업자가 반드시 알아야 할 것)
개발 환경의 네트워크 정책상 `cdnjs.cloudflare.com`(html2canvas가 로드되는 CDN)에 접근이 차단되어 있어,
**실제 이미지 캡처 성공 경로(검사 1~4번)를 이 환경에서는 검증하지 못했습니다.**
`html2canvas`가 로드되지 않은 상태에서 다운로드 버튼을 누르면 정상적으로 안내 문구가 뜨고 앱이 죽지 않는 것까지는 확인했지만,
**실제로 PNG 파일이 올바르게 생성되는지는 인터넷이 되는 일반 브라우저(또는 GitHub Pages 배포 환경)에서 직접 확인이 필요합니다.**

## 남은 일 (다음 작업자용)
- 검사 1~4번을 실제 인터넷이 되는 브라우저에서 검증
- 캡처된 이미지의 실제 품질 확인 (도넛차트의 `conic-gradient`, 커스텀 폰트가 html2canvas로 제대로 그려지는지는 라이브러리 특성상 별도 확인 필요할 수 있음)
