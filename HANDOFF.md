# 인계 문서 — 포트폴리오 배분 참고 도구 (PNG 다운로드 기능)

## 1. 목표
계산된 배분 결과(도넛차트·비율·금액·위험유형·예상수익률)를 **PNG 이미지 파일로 다운로드**할 수 있게 한다.

## 2. 현재 상태
- `index.html`에 `html2canvas`(CDN)를 이용한 다운로드 기능 구현 완료
- 결과 패널 상단에 "🖼️ 결과 이미지로 저장" 버튼 추가됨 (계산 전에는 버튼이 보이지 않음)
- 버튼 클릭 시: 슬라이더 등 조작 UI를 잠시 숨김 → `html2canvas`로 캡처 → PNG 다운로드 → UI 복원
- 계산 전 상태, 라이브러리 로딩 실패에 대한 예외 처리(가드) 포함
- **개발 환경 네트워크 제약으로 실제 이미지 캡처 성공 여부(검사 1~4)는 미검증** — 자세한 내용은 아래 "5. 남은 문제" 참조

## 3. 실행 명령
별도 빌드나 서버 설정이 필요 없는 순수 HTML/CSS/JS 파일입니다.

```
cd portfolio
python3 -m http.server 8000
```
브라우저에서 `http://localhost:8000/index.html` 접속.
(단순히 `index.html`을 더블클릭해 `file://`로 열어도 대부분 동작하지만, 위 방식을 권장합니다.)

**성공 확인 방법**: 설문 3개에 답하고 "배분 예시 계산하기" 클릭 → 결과 화면이 뜨면 정상. 이어서 "🖼️ 결과 이미지로 저장" 버튼을 눌러 PNG가 다운로드되면 이번 작업의 핵심 기능이 성공한 것입니다.

## 4. 통과한 검사 (10개 중 6개)
| # | 구분 | 내용 | 결과 |
|---|---|---|---|
| 5 | 오류 | 계산 전 다운로드 시도 → 버튼이 아예 노출되지 않음 | ✅ |
| 6 | 오류 | 금액 0/빈 값 → 기존 계산 검증 로직이 방어 | ✅ |
| 7 | 오류 | 다운로드 차단(라이브러리 로딩 실패로 대체 검증) → 안내 문구, 크래시 없음 | ✅ |
| 8 | 회귀 | 슬라이더 재분배(합계 100% 유지) | ✅ |
| 9 | 회귀 | 실시간 위험유형·수익률·변동성 갱신 | ✅ |
| 10 | 회귀 | "설문 다시하기" 버튼 | ✅ |

## 5. 남은 문제
**검사 1~4번(정상 케이스)이 미검증 상태입니다.**
```
1. 기본 배분 상태에서 다운로드 → PNG 파일이 저장된다
2. 슬라이더로 비중 조정 후 다운로드 → 조정된 최신 배분이 이미지에 반영된다
3. 투자금액을 다르게 입력 후 다운로드 → 금액 표시가 정확히 반영된다
4. 서로 다른 위험 프로필에서 각각 다운로드 → 배분·색상이 정확히 구분되어 저장된다
```
개발 중이던 환경이 `cdnjs.cloudflare.com`(html2canvas가 로드되는 CDN) 접근을 차단하고 있어서,
실제로 파일이 정상적으로 만들어지는지를 확인하지 못했습니다. 특히 다음이 실제로 우려되는 지점입니다:
- 도넛차트가 CSS `conic-gradient`로 그려지는데, `html2canvas`가 이걸 정확히 캡처하는지 브라우저별로 다를 수 있음
- Google Fonts(`Space Grotesk`, `Inter`, `JetBrains Mono`)가 캡처된 이미지에도 그대로 적용되는지 확인 필요

## 6. 다음 행동
1. 인터넷이 되는 일반 브라우저에서 위 검사 1~4번을 실제로 실행해서 확인
2. 문제가 있다면(예: 도넛차트가 캡처에서 깨짐) 원인을 파악해 수정
   - 참고: `html2canvas` 옵션에 `backgroundColor`, `scale: 2`가 이미 설정되어 있음 (`index.html` 내 `downloadImgBtn` 클릭 핸들러 부분)
3. 검사 1~10번 전체가 통과하면 완료
4. 완료 후 최종 커밋 메시지에 "검사 1~10 전체 통과"라고 명시해줄 것

## 7. 건드리면 안 되는 부분
- **위험도 계산 로직**(`computePortfolioStats`, `CORR` 상관계수 테이블, `RISK_BANDS` 임계값)은 이번 작업과 무관합니다. 이미 5단계 프로필이 정확히 순서대로 나오도록 검증·조정이 끝난 상태라 건드리지 마세요.
- **슬라이더 재분배 로직**(`onSliderChange`)도 별도 기능이라 이번 작업 범위 밖입니다.
- `capturing` CSS 클래스는 캡처 중에만 조작 UI를 숨기는 용도입니다. 이 클래스명을 다른 용도로 재사용하지 마세요.
