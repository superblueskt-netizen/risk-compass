# 인계 문서 — 포트폴리오 배분 참고 도구 (다운로드 파일명 개선)

## 1. 목표
결과 이미지를 다운로드할 때, 파일명에 위험유형과 투자금액이 포함되게 해서 여러 번 다운로드해도
구분할 수 있게 한다. (예: portfolio_result_위험중립형_1000만원_2026-08-25.png)

참고: 이전 목표였던 "PNG 다운로드 기능"은 이미 완료 및 실사용 검증까지 끝났습니다
(도넛차트가 안 나오던 버그도 발견해서 수정 완료). 자세한 히스토리는 WORKLOG.md 참조.

## 2. 현재 상태
- index.html의 다운로드 클릭 핸들러에서 파일명에 safeRiskName(위험유형)과 amountLabel(금액, 만원 단위)을
  포함하도록 이미 수정됨
- 파일명에 쓸 수 없는 특수문자(\ / : * ? " < > |)는 제거 처리됨
- 날짜는 YYYY-MM-DD까지만 포함되어 있어서, 같은 날 여러 번 다운로드하면 파일명이 겹침 (미해결)
- 도넛차트 캡처 기능 자체는 지난 세션에서 실사용 검증까지 끝난 상태라 이번 작업과 무관 — 건드리지 말 것

## 3. 실행 명령
별도 빌드나 서버 설정이 필요 없는 순수 HTML/CSS/JS 파일입니다.

```
cd portfolio
python3 -m http.server 8000
```
브라우저에서 http://localhost:8000/index.html 접속.

**성공 확인 방법**: 설문 답하고 금액 입력 → 계산 → "결과 이미지로 저장" 클릭 → 다운로드된 파일명에
위험유형과 금액이 들어있으면 이번 작업의 핵심은 이미 성공한 것. 남은 건 초 단위 중복 방지 처리.

## 4. 통과한 검사 (검사 11~14번 중 2개)
| # | 구분 | 내용 | 결과 |
|---|---|---|---|
| 11 | 정상 | 파일명에 위험유형이 포함된다 | 통과 (단위 테스트) |
| 12 | 정상 | 파일명에 금액이 만원 단위로 포함된다 | 통과 (단위 테스트) |

## 5. 남은 문제
- **검사 13 (오류)**: 같은 날 두 번 이상 다운로드하면 파일명이 겹쳐서 이전 파일을 덮어씀.
  날짜(YYYY-MM-DD)만으로는 유일성이 보장 안 됨.
- **검사 14 (회귀)**: 이번 파일명 변경이 기존 이미지 캡처(도넛차트 포함) 기능을 깨뜨리지 않는지
  실제 배포 환경에서 아직 확인 안 됨.

## 6. 다음 행동
1. index.html에서 "TODO(다음 작업자):" 주석을 찾을 것 (다운로드 클릭 핸들러 안에 있음)
2. 파일명의 날짜 부분(stamp)에 초 단위 시간을 추가해서 유일성을 보장할 것
   - 예: `2026-08-25` → `2026-08-25_153042` 같은 형태
   - 너무 복잡하게 만들 필요 없음, 초 단위 시각만 덧붙이면 충분
3. 로컬에서 여러 번 연속 다운로드해서 파일명이 겹치지 않는지 확인 (검사 13)
4. 실제 배포 환경(GitHub Pages)에서 다운로드해서 도넛차트가 여전히 정상 표시되는지 확인 (검사 14)
5. 검사 11~14번 전체 통과하면 완료. 최종 커밋 메시지에 "검사 11~14 전체 통과"라고 명시할 것

## 7. 건드리면 안 되는 부분
- **도넛차트 렌더링 로직**(drawDonut, resolveCssColor 함수)은 지난 세션에서 실사용 검증까지 끝난 상태입니다.
  건드리지 마세요.
- **위험도 계산 로직**(computePortfolioStats, CORR 상관계수 테이블, RISK_BANDS 임계값)도
  이번 작업과 무관합니다. 건드리지 마세요.
- **슬라이더 재분배 로직**(onSliderChange)도 별도 기능이라 이번 작업 범위 밖입니다.
- 파일명을 너무 길게 만들지 마세요. 위험유형+금액+날짜+시각 정도면 충분합니다.
