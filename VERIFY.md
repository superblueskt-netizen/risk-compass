# 짧은 확인 방법 (4줄)

> 2026-08-25 원 작업 종료 시점에 이미 작성해 로컬(`검증안내서_risk-compass.pdf`)로 보관하고 있던 내용을
> 그대로 저장소에 반영한 것입니다. 재검증(2026-09-01) 단계에서 새로 쓰지 않았습니다.

1. **위치**: https://superblueskt-netizen.github.io/risk-compass/ 를 엽니다. 설치나 로그인 없이 바로 열리고, 첫 화면에서 위험성향 설문이 보입니다. (두 AI의 작업 기록·인계 과정은 https://github.com/superblueskt-netizen/risk-compass 의 WORKLOG.md·HANDOFF.md·COMPARISON.md·커밋 히스토리에서 확인 가능합니다.)
2. **행동(3단계 이내)**: ① 설문 3개(투자 기간·손실 감내도·투자 경험)에 답하고 금액을 입력한 뒤 "배분 예시 계산하기" 클릭 → ② 결과 화면에서 슬라이더를 하나 움직여 비중을 조정 → ③ "🖼️ 결과 이미지로 저장" 클릭.
3. **통과 모습**: ①에서 계산 즉시 도넛차트·위험유형·예상 수익률·변동성이 화면에 나타난다. ②에서 슬라이더를 움직이면 나머지 비중이 자동으로 재분배되고 위험유형·수익률·변동성이 실시간으로 다시 계산된다. ③에서 다운로드된 PNG를 열면 화면에 보이던 도넛차트·배분표가 그대로 담겨 있고, 파일명에 위험유형·투자금액·다운로드 시각(초 단위)이 포함되어 있어 여러 번 받아도 파일이 겹치지 않는다 — 실제로 5초 안에 두 번 연달아 다운로드해도 시각(HHMMSS) 부분이 달라 두 파일 모두 남는다.
4. **안 될 때 모습**: 계산 버튼이 안 눌리면(설문·금액 중 하나라도 비어 있으면 안내 문구만 뜸), 이미지 저장이 안 되면(시크릿 모드 등 다운로드 차단 환경일 수 있음, 인터넷 연결 확인 후 새로고침), 도넛차트가 이미지에서 안 보이면(강력 새로고침 Ctrl+Shift+R 후 재시도), 슬라이더를 움직여도 비중 합계가 100%가 아니면 — 이 중 하나라도 재현되면 버그입니다.
