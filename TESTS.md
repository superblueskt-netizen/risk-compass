# 고정 검사 14개 — ID·입력·기대값

T05 실습(카드1)에서 정한 검사 목록입니다. 검사 1~10은 "PNG 다운로드" 기능(AI A 담당),
검사 11~14는 "다운로드 파일명 개선" 기능(AI A 착수 → AI B 완료)에 대한 것입니다.
대상 페이지: `index.html` (https://superblueskt-netizen.github.io/risk-compass/)

| ID | 구분 | 입력 | 기대값(관찰 가능) |
|---|---|---|---|
| 1 | 정상 | 설문 3문항에 임의 응답(예: 3/3/3, 점수 9 → 적극투자형), 금액 기본값(10,000,000원) 유지 → "배분 예시 계산하기" → "🖼️ 결과 이미지로 저장" 클릭 | PNG 파일이 다운로드되고, 파일 안에 도넛차트(색이 채워진 원)와 배분 목록이 보인다 |
| 2 | 정상 | 계산 완료 후 주식 슬라이더를 65%→80%로 이동한 뒤 다운로드 | 저장된 이미지의 배분 비율이 조정된 값(주식 80%대)을 반영한다 |
| 3 | 정상 | 계산 완료 후 금액 입력칸을 "5,500,000"으로 바꾸고(슬라이더 건드리지 않음) 다운로드 | 파일명에 `550만원`이 포함된다 |
| 4 | 정상 | 설문 점수를 3~12 범위 내 5개 구간(안정형~공격투자형)에 각각 해당하도록 답변을 바꿔가며 5회 계산·다운로드 | 매번 파일명의 위험유형 표기가 화면에 표시된 프로필명과 정확히 일치한다 |
| 5 | 오류 | 설문에 답하지 않은 초기 상태(#resultSection 숨김)에서 다운로드 버튼 클릭을 강제로 발생시킴 | `currentAlloc`이 없어 "먼저 설문을 완료하고 배분을 계산해주세요." 알림이 뜨고 다운로드가 시작되지 않는다 |
| 6 | 오류 | 금액 입력칸을 비운 채(0원) "배분 예시 계산하기" 클릭 | "투자 예정 금액을 입력해주세요." 알림이 뜨고 결과 화면이 열리지 않는다 |
| 7 | 오류 | 시크릿 창 등 다운로드가 차단된 환경에서 정상 계산 후 다운로드 클릭 | "이미지 저장에 실패했습니다..." 알림이 뜨고, 버튼이 다시 활성화되며 원래 문구로 돌아온다(앱이 멈추지 않음) |
| 8 | 회귀 | 계산 후 임의 슬라이더 하나를 40%로 변경 | 나머지 자산 비중 합이 60%가 되도록 기존 비율대로 자동 재분배되고, 5개 자산 비중의 합이 정확히 100% |
| 9 | 회귀 | 슬라이더 조정 | "현재 배분의 위험유형"·"최종 예상 연 수익률"·"조합 변동성" 값이 조정 즉시(리렌더 없이) 새 배분 기준으로 갱신된다 |
| 10 | 회귀 | 결과 화면에서 "↺ 설문 다시하기" 클릭 | 결과 섹션이 숨겨지고 설문 패널로 스크롤 이동한다 |
| 11 | 정상 | 임의 설문 응답 후 계산 → 다운로드 | 파일명에 위험유형 이름(예: `위험중립형`)이 포함된다 |
| 12 | 정상 | 투자금액 10,000,000원으로 계산 → 다운로드 | 파일명에 `1000만원`이 포함된다(만원 단위 축약) |
| 13 | 오류(경계) | 계산 완료 후 다운로드 버튼을 5초 이내 간격으로 2번 클릭 | 두 파일명이 서로 다르다(시각 HHMMSS 부분이 다름) — 이전 파일을 덮어쓰지 않는다. *같은 초 안에 2번 클릭하면 여전히 충돌 가능하며, 이는 알려진 한계로 남긴다* |
| 14 | 회귀 | 파일명 로직 변경 이후 다운로드 | 저장된 PNG를 열어보면 도넛차트가 여전히 정상적으로(빈 원이 아니라 색이 채워진 상태로) 캡처되어 있다 |

## 비고
- 검사 1~10은 AI A(Claude)가 구현·통과시켰고, 실제 배포 환경에서 검사 1~4 수행 중 도넛차트 캡처 버그를 발견해 직접 수정했습니다(WORKLOG.md 참조).
- 검사 11~12는 AI A가 인계 전 단위 테스트로 통과, 검사 13~14는 AI B(Gemini)가 인계 문서만 보고 완료·재검증했습니다(HANDOFF.md, WORKLOG.md 참조).
- 본 문서는 재검증(2026-09-01) 단계에서, 이미 진행됐던 검사 설계(WORKLOG.md의 "구분/결과" 표)에 입력값·기대값 열을 명시적으로 보강한 것입니다 — 검사의 대상·구분·번호 자체는 최초 작업 시작 시점(커밋 `ce6012f`~`8331656`)에 이미 정해져 있었습니다.
